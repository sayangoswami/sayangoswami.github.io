---
title: "Module 7: Applications & Capstone"
subtitle: "GPU Applications, Industry Context, and the Capstone — COSC 435"
format: revealjs
---

<!--
INSTRUCTOR NOTES
================
PROMOTED this session (full rewrite/restructure). The prior deck ("Module 8:
The Capstone", renumbered to Module 7 earlier this engagement) covered the
capstone spec end-to-end but had ZERO content for three of this module's
four lecture sessions — schedule.md commits Module 7 to four distinct
lecture topics (Nov 30 image-processing pipeline, Dec 2 ML inference/cuDNN/
LLMs, Dec 7 industry case studies, Dec 9 a work session) plus the capstone
itself, and only the capstone-spec material existed anywhere in the repo.
That's now restructured into five dated sessions matching schedule.md
exactly, with three sessions of genuinely new content (Nov 30, Dec 2, Dec 7)
and the pre-existing capstone-spec content organized under Dec 4 (the
"Capstone Lab 1... Team kickoff" in-class day) as one comprehensive spec
walkthrough, since it doesn't split cleanly across dates and Dec 4 is the
natural in-class moment for it. Dec 9 ("Capstone work session — professor
circulating") is intentionally a single light pointer slide, not a full
lecture — it's a work/Q&A session by schedule.md's own description.

None of Nov 30/Dec 2/Dec 7's content maps to a competency ID, checkpoint, or
quiz question (confirmed via syllabus.md and checkpoint-competency-map.md) —
consistent with how this course treats other schedule.md-committed context
lectures (Module 1's historical content, Module 6's Nov 23 ecosystem
survey): real, substantive material, since slides are students' only
resource, but explicitly not separately assessed.

Also fixed while promoting: every speaker note in this deck previously had
zero suggested-time budgets (the one standard this deck violated that no
other promoted module did) — added throughout. All [CONTENT] placeholder
notes resolved: genuinely explanatory content (the academic-integrity policy
on Part G, the Part F worked example) moved onto visible slides; pure
delivery guidance kept in notes with a time budget added.
-->

# Session 1 · Nov 30 · GPU-Accelerated Image Processing

## Today's Plan

- A complete, realistic pipeline: load an image, process it on the GPU, get the result back
- Where each stage of that pipeline maps to what you've already learned this semester
- Why this exact shape — load → transfer → compute → transfer → save — is also the capstone's shape

::: {.notes}
~1 min. Full 50-min session, no quiz. This is a synthesis lecture — the goal is showing students that Modules 2-6 already taught them everything needed for a complete, real pipeline, not introducing new mechanics.
:::

---

## The Full Pipeline, End to End

```
Disk (JPEG/PNG file)
   │  decode — turn compressed bytes into a raw pixel array
   ▼
Host (NumPy array, shape (H, W, 3), uint8)
   │  cuda.to_device() / cp.asarray()          ← Module 2 / Module 6
   ▼
Device (GPU memory)
   │  process — one or more GPU kernels or CuPy ops
   ▼
Device (GPU memory, transformed)
   │  copy_to_host() / cp.asnumpy()            ← Module 2 / Module 6
   ▼
Host (NumPy array)
   │  encode — turn the array back into a file format
   ▼
Disk (result image) or display
```

- Every arrow in this diagram is something you've already written this semester — a decode/encode step (ordinary Python, not GPU-specific) bookending a host↔device transfer and a GPU compute stage
- The "process" stage is where your semester's kernel-writing skills (Modules 2-5) or library calls (Module 6) actually do the work

::: {.notes}
~3 min. This diagram is worth drawing on the board from memory — it's the same "three-step cycle" from Module 6's CuPy lecture (transfer, compute, transfer back), just with real disk I/O bookending it this time.
:::

---

## Stage 1: Load and Decode

```python
from PIL import Image
import numpy as np

img = Image.open("photo.jpg")            # decode — ordinary CPU work, not GPU
arr = np.array(img, dtype=np.uint8)      # shape (H, W, 3) — a NumPy array
print(arr.shape, arr.dtype)
```

- Decoding a compressed image format (JPEG, PNG) into a raw pixel array happens on the **CPU** — this step has nothing to do with the GPU, and there's no reason to change that
- The result is an ordinary NumPy array — a 3D array of `(height, width, channels)` — exactly the kind of array-shaped data every GPU technique this semester has assumed
- This is also where a real pipeline decides on a **batch**: loading and processing many images together, rather than one at a time, to better amortize GPU overhead (Module 3's fixed-overhead-vs-problem-size lesson, applied directly)

::: {.notes}
~2 min. Worth naming explicitly: decoding is *not* a GPU task — some pipelines do move decoding to the GPU (NVIDIA's nvJPEG library exists for exactly this), but that's beyond this course's scope. The point here is recognizing which stages are naturally CPU work and which are naturally GPU work.
:::

---

## Stage 2: Transfer and Process

```python
import cupy as cp

arr_gpu = cp.asarray(arr)                       # host → device (Module 6)

# Grayscale: a weighted sum across the color channels — an element-wise op
gray_gpu = (0.299 * arr_gpu[:, :, 0]
          + 0.587 * arr_gpu[:, :, 1]
          + 0.114 * arr_gpu[:, :, 2])

# Box blur: a 2D stencil, exactly Module 3's box-blur kernel — could be
# written as a @cuda.jit kernel instead of a CuPy call, same underlying idea
blurred_gpu = box_blur_2d_kernel(gray_gpu, radius=2)
```

- Grayscale conversion is a straightforward element-wise operation — the same "one thread/one CuPy op, one output" shape used everywhere since Module 2
- Blurring is a **stencil** (Module 3) — each output pixel depends on a neighborhood of inputs, with a boundary condition needed at the image edges
- A real pipeline mixes both styles freely: CuPy for standard operations, a hand-written Numba kernel for anything with no library equivalent — exactly Module 6's CuPy-vs-Numba decision, applied to image processing specifically

::: {.notes}
~4 min. This slide's whole point is recognition: grayscale = Module 2's element-wise pattern, blur = Module 3's stencil pattern, applied to real pixel data instead of a synthetic array. Nothing here is new syntax.
:::

---

## Stage 3: Transfer Back and Save

```python
result_cpu = cp.asnumpy(blurred_gpu)             # device → host (Module 6)
result_img = Image.fromarray(result_cpu.astype(np.uint8))
result_img.save("blurred_photo.jpg")             # encode — CPU work again
```

- The device→host transfer is the same call from Module 6's three-step cycle — and, per Module 4's PCIe-bottleneck lesson, this is the most expensive single step in the whole pipeline per byte moved
- Saving (encoding back to a compressed format) is CPU work again, mirroring the load stage
- **The pipeline-design lesson**: minimize the *number* of host↔device round trips. If you had five processing steps to apply, you'd transfer once, run all five GPU operations back-to-back, and transfer back once — not transfer, process, transfer back, transfer again, process, transfer back (Module 2's "minimize transfers" rule, applied to a realistic multi-step pipeline)

::: {.notes}
~2 min. This is the practical payoff of the whole session — the diagram's five stages exist to set up this one design principle, which transfers directly to the capstone's own pipeline (next session's framing, and Dec 4's spec walkthrough).
:::

---

## This Week

- **Dec 2 (next lecture):** ML inference on GPU — the same load→transfer→process→transfer pipeline shape, applied to a neural network instead of an image filter
- This pipeline shape — and the "minimize transfers" lesson specifically — is exactly the shape the capstone's own pipeline takes (embeddings in, similarity computed on GPU, top-K results out)

::: {.notes}
~1 min. Closing pointer slide.
:::

---

# Session 2 · Dec 2 · ML Inference on GPU

## Today's Plan

- The same pipeline shape from Nov 30, applied to a neural network
- How PyTorch actually moves data for real models — beyond the single-tensor case from Module 6
- What cuDNN does, and why it matters specifically for neural-network workloads
- Batching strategy, and the connection to large language models (LLMs)

::: {.notes}
~1 min. Full 50-min session, no quiz. Builds directly on Module 6's Nov 18 PyTorch session — that session taught the mechanics (tensors, `.to('cuda')`, forward pass); this session is about doing that mechanics *well*, at realistic scale.
:::

---

## Recap: The Forward Pass, at Toy Scale

```python
x_gpu = x_cpu.to('cuda')
with torch.no_grad():
    logits_gpu = model(x_gpu)
logits_cpu = logits_gpu.cpu()
```

- Module 6 (Nov 18) taught this exact pattern on a tiny, single batch of 32 samples — enough to learn the mechanics, not enough to reflect how inference actually runs in production
- Today's question: what changes when the model is large, the data keeps arriving continuously, and performance actually matters?

::: {.notes}
~2 min. Brief recap slide — don't re-teach Module 6's content, just anchor today's material against it.
:::

---

## How PyTorch Really Moves Data

- Real inference systems don't call `.to('cuda')` on one batch and stop — they continuously load new data, moving it to the GPU while the *previous* batch is still being processed
- PyTorch's `DataLoader` with `pin_memory=True` allocates host memory in a way that makes the host→device transfer (Module 2's PCIe bottleneck, again) faster and lets it overlap with GPU computation, instead of the CPU and GPU strictly taking turns
- This is the same idea as Module 2's "minimize and overlap transfers" lesson, implemented for you by a library rather than hand-written — recognizing the underlying problem is more valuable than memorizing this specific API

::: {.notes}
~3 min. Keep this at the concept level — `pin_memory` and overlapped transfer are real PyTorch features, but this course doesn't require students to write this code. The goal is recognizing "oh, this is Module 2's transfer-minimization lesson, industrialized."
:::

---

## What cuDNN Does

- **cuDNN** (mentioned in Module 6's library-ecosystem slide) is NVIDIA's library of hand-tuned neural-network building blocks — convolutions, pooling, activation functions, normalization layers
- A convolution (the core operation in image-processing neural networks) can be computed several genuinely different ways on a GPU — as a direct sliding-window stencil (Module 3's territory), or by reformulating it as a matrix multiply (Module 6's `@`/cuBLAS territory), among others — and the fastest choice depends on the exact layer shape
- cuDNN automatically benchmarks several implementations for your specific layer sizes and picks the fastest one at runtime — the same "which tool for this job" judgment call from Module 6 (CuPy vs. Numba), except cuDNN makes the call *for you*, per operation, automatically
- PyTorch calls into cuDNN under the hood every time you run a convolutional layer — you never see cuDNN directly, but it's doing real work every forward pass

::: {.notes}
~3 min. This directly connects Module 3's stencil framing and Module 6's library-selection framing to a single real example: a convolution genuinely *is* a stencil, computed by a library that's smart enough to also compute it as a matmul when that's faster.
:::

---

## Batching Strategy

[DIAGRAM] — Two small bar charts side by side, sharing a y-axis labeled "time." *Left, "batch size = 1":* many thin, short bars in a row, each representing one input processed alone, with a large gray "launch overhead" segment at the base of every single bar (the overhead repeats every time). *Right, "batch size = 32":* one much taller bar representing 32 inputs processed together, with a single "launch overhead" segment at its base — proportionally tiny compared to the much larger "useful compute" segment above it. Caption under both: "Same total work, very different overhead-to-work ratio."

- Running inference one input at a time badly underuses a GPU — the same "problem too small to fill the device" failure mode from Module 1's "when does the GPU lose" slide
- **Batching**: group many inputs together into one larger tensor, and run the forward pass once on the whole batch — turning many small, overhead-dominated launches into one large, throughput-dominated one
- Larger batches generally mean better GPU utilization and higher throughput (more total inferences per second) — but also higher **latency** for any single input (it has to wait for the rest of its batch to arrive) and more GPU memory used at once
- This is a direct, concrete instance of Module 1's latency-vs-throughput tradeoff: a production system tunes batch size deliberately, trading a little per-request latency for much better aggregate throughput

::: {.notes}
~3 min. Tie explicitly back to Module 1's very first architectural framing — latency-optimized vs. throughput-optimized wasn't just about comparing CPU to GPU, it's a design knob *within* GPU workloads too, and batch size is exactly that knob for inference.
:::

---

## The LLM Connection

- Large language models (the kind behind chatbots and code assistants) are, at their computational core, an enormous, repeated sequence of matrix multiplies — the same `@` operator from Module 6, just at a scale of billions of parameters
- Generating text one token at a time (autoregressive generation) means running a forward pass repeatedly, once per output token — each pass is itself a batch-and-inference problem exactly like this session's content, just chained many times in a row
- LLM inference is famously **memory-bandwidth-bound**, not compute-bound (Module 1's arithmetic-intensity preview, Module 4's roofline model) — moving the model's billions of parameters from GPU memory to the compute units, over and over, is often the actual bottleneck, not the arithmetic itself
- This is why LLM-serving hardware emphasizes memory bandwidth and capacity as much as raw FLOPS — a direct, large-scale echo of Module 1's memory-wall lesson from week 3

::: {.notes}
~3 min. This is the "why does everyone care about GPUs right now" payoff, made concrete with the actual dominant workload — tie it back explicitly to arithmetic intensity (Module 1/4) so it reads as an application of a concept already taught, not a new fact to memorize.
:::

---

## This Week

- **Dec 4:** Capstone Lab 1 — a full walkthrough of the capstone spec, both milestones, and what's expected of your team
- **Dec 7 (following lecture):** A survey of GPU computing across industries — training, rendering, genomics, finance

::: {.notes}
~1 min. Closing pointer slide.
:::

---

# Session 3 · Dec 4 · Capstone Lab 1 — The Spec Walkthrough

## Today's Plan

- The capstone problem, revisited now that you can actually build it
- Both milestones (CK-13, CK-14) — what each one asks for, and the code you'll write
- Team structure and how grading works
- What to do before you leave class today

::: {.notes}
~1 min. In-class work day (per schedule.md: "Team kickoff. Problem selection, data prep, notebook scaffold"). CK-13 is due Dec 1, three days before this session — most teams will already have started; use today for questions, troubleshooting, and previewing CK-14, not a cold-open introduction. Budget the bulk of class time for hands-on work after this walkthrough, not lecture.
:::

---

## The Day-1 Teaser, Revisited

> *Every song is described by 64 numbers. A user is also described by 64 numbers. We measure how well a song matches a user by multiplying those 64 pairs of numbers together and adding them up. We need to find the 10 best matches for 1,000 users, from a library of 100,000 songs, in under a second.*

You now know everything you need to do this.

::: {.notes}
~2 min. Pause here. This slide should feel different today than it did on the first day. Students heard this in week 1 and could not have written a single line of GPU code. Now they can implement it in its entirety.
:::

---

## What You Are Building

A **k-nearest-neighbor recommendation engine**:
- Database: 100,000 items (songs, movies, products), each a 64-dimensional embedding
- Query: 1,000 users, each a 64-dimensional embedding
- Task: for each user, find their top-K most similar items
- Metric: **cosine similarity** — but since every vector is pre-normalized (length 1), cosine similarity reduces to a plain **dot product**, no separate distance formula needed

::: {.notes}
~2 min. This is the one conceptual thing to land before the math slide: "cosine similarity" sounds like it needs a scary formula, but with pre-normalized vectors it's just a dot product — the setup cell handles normalization, students never derive it themselves.
:::

---

## The Mathematics (The Friendly Version)

Pre-normalized vectors: each `||v||₂ = 1.0`

$$\text{similarity}(u, v) = u \cdot v = \sum_{k=0}^{63} u_k \cdot v_k$$

All 1,000 × 100,000 similarities at once:

```python
sim = Q_mat @ D_mat.T    # shape (1000, 100_000)
```

Top-K per user:

```python
top_k = cp.argpartition(-sim, K, axis=1)[:, :K]
```

- The entire mathematical core of the capstone is these two lines — the capstone's actual difficulty is building and optimizing the GPU kernel that computes the first line efficiently, and understanding every design decision along the way, not the math itself

::: {.notes}
~2 min. Reassurance slide: if a team is intimidated by "cosine similarity" and "k-nearest-neighbor," this is the moment to show them it's two lines of code they already know how to write.
:::

---

## The Capstone Pipeline, End to End

[DIAGRAM] — A flowchart mirroring Nov 30's image-processing pipeline shape. Top box: "Q_mat (1000×64) and D_mat (100,000×64) — host NumPy arrays." Arrow down, labeled "cuda.to_device() (CK-13) or cp.asarray() (CK-14) — Module 2 / Module 6," into a box labeled "[GPU] sim = Q @ D.T — shape (1000, 100,000)" with a sub-label "CK-13: naive Numba kernel · CK-14: tiled Numba kernel." Arrow down into a second GPU-side box labeled "[GPU] top_k = cp.argpartition(-sim, K, axis=1)[:, :K] — CuPy." Arrow down, labeled "cp.asnumpy() / copy_to_host()," into a final box labeled "Top-K recommendations per query — back on the host."

- Every box and arrow in this diagram is something you can already label from this semester — this is Nov 30's "load → transfer → process → transfer → save" pipeline shape, with the capstone's specific operations dropped in
- CK-13 does the middle box with a hand-written naive kernel; CK-14 replaces it with the tiled kernel from Module 4, and both use CuPy for the top-K step (Module 6)

::: {.notes}
~2 min. Have a team try to label this diagram from memory before revealing the code labels — if they can, they understand the pipeline structure, which is most of what CK-13 Part A/Part C actually test.
:::

---

## CK-13: Milestone 1 (Due Dec 1, 25 pts)

**Goal**: a working, correct GPU k-NN pipeline

- Part A (7 pts): Conceptual questions — what do dot products measure? Why is this a GPU problem?
- Part B (9 pts): Baseline NumPy implementation (`@`, `np.argsort`)
- Part C (9 pts): Naive 2D CUDA kernel with 3 fill-in-the-blank gaps

**Deliverable**: correct top-K results vs. NumPy baseline

::: {.notes}
~2 min. CK-13 is the "it works" milestone — correctness is the bar, not speed. Students who finish CK-13 have a functioning GPU recommendation engine, even if it's slow. CK-14 is entirely about making it fast and understanding why.
:::

---

## CK-13 Part C: The Kernel

```python
@cuda.jit
def sim_naive(Q, D, sim):
    row, col = cuda.grid(2)
    if ___GAP_1___:                      # bounds guard
        acc = 0.0
        for k in range(___GAP_2___):     # inner loop over D
            acc += Q[row, k] * D[col, k]
        sim[___GAP_3___] = acc           # write result
```

- This is a direct application of Module 2's 2D-kernel pattern — `cuda.grid(2)`, a bounds guard, an inner accumulation loop — with the specific similarity computation from the slide above filled in

::: {.notes}
~2 min. Gap 1: `row < Q.shape[0] and col < D.shape[0]`. Gap 2: `Q.shape[1]` (the embedding dimension, 64). Gap 3: `row, col`. Students fill these in, launch the kernel, and verify against their own NumPy baseline from Part B — the same self-verification habit from every checkpoint this semester.
:::

---

## CK-14: Milestone 2 (Due Finals Week, 50 pts)

**Goal**: an optimized, characterized, and reflected-upon pipeline

- Part A (14 pts): Tiled similarity kernel (3 gaps: shared arrays, two syncs)
- Part B (6 pts): CuPy `argpartition` top-K
- Part C (8 pts): Correctness verification
- Part D (8 pts): Speedup table across query counts
- Part E (6 pts): TILE sweep {8, 16, 32} + written analysis
- Part F (6 pts): One additional optimization (negative result + explanation = full credit)
- Part G (6 pts): Individual written reflection per team member

::: {.notes}
~2 min. CK-14 is where students move from "it works" to "I understand why it works and what its limits are." Part G ensures both partners engage individually even on a joint submission — more on this on the Team Structure slide.
:::

---

## CK-14 Part A: The Tiled Kernel

```python
TILE = 16

@cuda.jit
def sim_tiled(Q, D, sim):
    tile_Q = ___GAP_1a___    # shared array (TILE, TILE), float32
    tile_D = ___GAP_1b___    # shared array (TILE, TILE), float32

    row = cuda.blockIdx.x * TILE + cuda.threadIdx.x
    col = cuda.blockIdx.y * TILE + cuda.threadIdx.y
    tx, ty = cuda.threadIdx.x, cuda.threadIdx.y
    acc = 0.0

    for t in range((Q.shape[1] + TILE - 1) // TILE):
        # Load tiles ...
        ___GAP_2___      # syncthreads after loading
        # Compute partial dot product ...
        ___GAP_3___      # syncthreads before next tile

    if row < sim.shape[0] and col < sim.shape[1]:
        sim[row, col] = acc
```

- This is Module 4's tiled-matmul kernel (Oct 28), with the accumulation swapped from a full matrix multiply to the similarity computation above — same shared-memory declarations, same two-`syncthreads()` structure

::: {.notes}
~2 min. Gap 1a/1b: `cuda.shared.array((TILE, TILE), numba.float32)`. Gap 2: `cuda.syncthreads()`. Gap 3: `cuda.syncthreads()`. Students who worked through Module 4 thoroughly will recognize this immediately — if a team is stuck here, point them back to Module 4's "Why Two syncthreads()?" slide before re-explaining from scratch.
:::

---

## Team Structure

- Teams of 2 (one trio if enrollment is odd — see the Trio Variant below)
- Both partners submit the same notebook, with both names in the header cell
- **Part G is graded individually within the joint submission** — each partner writes their own labeled reflection cell
- One partner writing both reflections and swapping names is an academic integrity violation, not a shortcut — graders read for voice and content differences between the two reflections specifically

::: {.notes}
~2 min. Say the academic-integrity point plainly and once, not as a threat — most violations of this kind come from teams not realizing it's individually graded at all, not from bad intent. Stating it clearly here prevents that.
:::

---

## The Trio Variant

If your team has three members:
- Database size: 250,000 items (vs. 100,000 for pairs)
- K: 20 nearest neighbors (vs. 10 for pairs)
- Otherwise: identical structure and point values

- The larger problem is a genuine (if slight) advantage, not a penalty: more data means the GPU's fixed launch overhead is amortized over more useful work, so speedups tend to be more dramatic and easier to characterize in Part D and E

::: {.notes}
~1 min. Worth saying explicitly for any trio team that might otherwise feel like they drew the short straw — the scaling actually works in their favor for the speedup-measurement parts of CK-14.
:::

---

## Grading Philosophy

| | CK-13 | CK-14 |
|---|---|---|
| Points | 25 | 50 |
| Key bar | Correctness | Correctness + characterization |
| Partial credit | Yes, per part | Yes, per part |
| Negative results | Not applicable | Full credit in Part F, with good explanation |

::: {.notes}
~1 min. Point at the last row specifically before moving to the next slide — it's the one line in this table students tend to disbelieve until they see the worked example.
:::

---

## Part F: The Optimization Challenge

You must try **one optimization** beyond the tiled kernel:

- Loop unrolling
- Vectorized loads
- Double buffering
- Changing TILE size (if not already covered in Part E)
- Something from the literature

**A negative result with good analysis earns full credit.** For example: *"I tried unrolling the inner loop and it was 3% slower because the compiler already applies this optimization automatically"* — with timing evidence — is full credit. *"I tried it and it didn't work"* with no data is not.

::: {.notes}
~3 min. The point of Part F is not to find a magic speedup — it's demonstrating that you understand *why* an optimization does or doesn't help. A student who tries something and explains the result thoughtfully has learned more than one who stumbles onto a 10% gain with no understanding of why. Say this explicitly; it's the single most reassuring thing you can tell an anxious team here.
:::

---

## The Bigger Picture

The capstone is a simplified version of:
- Spotify's audio embedding search
- Netflix's collaborative filtering inference
- Amazon's "customers who bought" engine
- Any vector database retrieval system

The gap between your 100K-item capstone and a billion-item production system is:
- Approximate nearest neighbor algorithms (FAISS, ScaNN) instead of exact search
- Distributed GPU clusters instead of one GPU
- Model freshness and serving infrastructure
- **Not a different algorithm** — the dot-product-and-top-K core is identical

::: {.notes}
~2 min. EL-04 (already completed, due Nov 1) covered this gap via the Spotify and Netflix readings — this slide is the payoff of that reading, made concrete now that students have implemented the algorithm themselves.
:::

---

## Energy and Ethics (EL-05, Already Assigned)

Recall EL-05 (due Nov 22 — before this module even started):

- Strubell et al. (2019): training one large NLP model ≈ CO₂ of five trans-American flights
- A recent news article on AI data-center energy demand
- Prompt 3 asked: *would you take a job at a company running a recommendation engine at a scale that consumes as much electricity as a small town?*

**Speed and scale are not ethically neutral.** By the time you submit CK-14, this framing should be in the back of your mind — the ethical dimension is not separate from the technical work, it's the same work seen from a different angle.

::: {.notes}
~2 min. EL-05 was deliberately assigned early (Nov 22) rather than clustered near CK-14's finals-week deadline, specifically so this framing has time to sit with students throughout the whole build, not just as a last-minute add-on.
:::

---

## Key Dates

| Date | Event |
|---|---|
| EL-04 (Nov 1) | Recommendation-at-scale reading — already due |
| EL-05 (Nov 22) | Energy and ethics reading — already due |
| Nov 23 | Capstone spec distributed; teams declared |
| Dec 1 | **CK-13 due** — Milestone 1: working pipeline |
| Dec 4 (today) | Capstone Lab 1 — in-class work session |
| Dec 11 | Capstone Lab 2 — in-class demo day |
| Finals week | **CK-14 due** — Milestone 2: optimized pipeline |

::: {.notes}
~1 min. Fill in the exact finals-week date from the registrar calendar once available. The ~3-week gap between CK-13 (Dec 1) and CK-14 (finals week) is intentional — enough time to make meaningful optimizations with a working pipeline already in hand. Dec 4 and Dec 11 are in-class work/demo days connected to the milestones, not the deadlines themselves (see schedule.md's own note on this).
:::

---

## What to Do Right Now

1. If you don't already have a team, form one before you leave today
2. Make sure your Colab GPU runtime is working — teams lose real time to Colab access issues every semester; resolve this now, not at 11pm the night before something is due
3. Run the CK-13 setup cell: does `D_mat` have shape `(100_000, 64)`?
4. Start Part A's conceptual questions together, out loud, before splitting up the coding work

::: {.notes}
~2 min. This is the actionable close of the spec walkthrough — leave real class time after this slide for teams to actually do these four things, not just hear about them.
:::

---

# Session 4 · Dec 7 · GPU Computing in Industry

## Today's Plan

- Four brief case studies: how GPU computing shows up in ML training, rendering, genomics, and finance
- What each one has in common with something you've already built this semester

::: {.notes}
~1 min. Full 50-min session, no quiz. Keep each case study brisk (roughly 8-10 min) — the goal is breadth and recognition, not depth in any one field.
:::

---

## Case Study: ML Training

- Training a large model means repeatedly running the forward pass (Dec 2's territory) *and* computing gradients (autograd, briefly introduced Module 6) across enormous datasets, for many GPUs, for days or weeks
- The core operation, repeated billions of times, is still the matrix multiply — Module 6's `@`/cuBLAS, at a scale where even small per-operation inefficiencies compound into real time and cost
- Multi-GPU training adds a new problem this course hasn't covered: GPUs must periodically synchronize their results with each other over a network — a much slower and more complex version of Module 2's host↔device transfer, now between multiple devices instead of one

::: {.notes}
~2 min. This connects directly to Module 1's "a single training run can use thousands of GPUs for weeks" slide and Dec 2's LLM content — training is the other half of that picture, generation (inference) being the half already covered.
:::

---

## Case Study: Rendering

- Real-time ray tracing and path tracing (how modern game and film rendering works) is fundamentally a massively parallel, per-pixel or per-ray computation — the same "one thread, one independent unit of work" shape from Module 2, applied to light rays instead of array elements
- Rendering workloads are often extremely divergent (Module 2's Sep 30 content): different rays can bounce very different numbers of times before terminating, meaning different threads in the same warp frequently disagree on how much work remains
- This is precisely why divergence-handling (predication, the cost of mismatched branches) matters so much in graphics-focused GPU work — it's not just an exam topic, it's the daily reality of writing a renderer

::: {.notes}
~2 min. Good moment to note that this course's CUDA-focused, compute-only framing deliberately skipped graphics-specific GPU features (rasterization, texture units) — rendering workloads use the same underlying hardware and many of the same programming concepts, just with a different problem shape and additional graphics-specific hardware in play.
:::

---

## Case Study: Genomics

- DNA/protein sequence alignment — finding where a short sequence matches within a much longer reference — has the same shape as Module 5's parallel patterns: many independent comparisons that can run simultaneously, sometimes combined via a reduction to find the best-scoring match
- Protein structure prediction (the class of problem AlphaFold addresses) relies heavily on large matrix operations and neural-network inference — Module 6's `@` and PyTorch content, at a much larger and more specialized scale
- Genomics datasets are enormous (a single human genome is billions of base pairs) — exactly the kind of scale where Module 1's "GPU wins when there's enough work to amortize overhead" reasoning applies cleanly

::: {.notes}
~2 min. If time allows, ask whether anyone has used or heard of AlphaFold — it's a widely-known example that makes "neural network inference, GPU-accelerated, at genomics scale" concrete rather than abstract.
:::

---

## Case Study: Finance

- Monte Carlo simulation (running a random model millions of times to estimate a risk or a price) is embarrassingly parallel in the exact sense Module 1 defined it — each simulated trial is fully independent of every other one
- Module 5's random-number generation and reduction patterns (CuPy's GPU-side RNG, tree reduction to aggregate results) map directly onto this: generate millions of random scenarios on the GPU, then reduce them down to a summary statistic (an average price, a risk estimate)
- High-frequency trading systems care intensely about **latency**, not just throughput — a reminder that Module 1's latency-vs-throughput framing isn't just "GPU vs. CPU," it's a real design axis within GPU-accelerated systems too, echoing Dec 2's batching-strategy discussion

::: {.notes}
~2 min. Nice closing case study for the module — it reuses nearly every major concept from the semester (embarrassingly parallel, RNG, reduction, latency-vs-throughput) in one recognizable real-world context.
:::

---

## This Week

- **Dec 9 (next lecture):** Capstone work session — no new lecture content, bring your questions
- **Dec 11:** Capstone Lab 2 — in-class demo day
- **Finals week:** CK-14 due, and the Final Exam (comprehensive, all modules)

::: {.notes}
~1 min. Closing pointer slide.
:::

---

# Session 5 · Dec 9 · Capstone Work Session

## No New Content Today

- This is an open work session — the professor circulates to answer questions and help debug, and class time also includes a Final Exam review
- Come with your team's notebook open and specific questions ready — "my tiled kernel gives the wrong answer" is more useful to bring than "I'm stuck," since it lets troubleshooting start immediately
- If your team hasn't started CK-14 Part A (the tiled kernel) yet, today is the day — Dec 11's Capstone Lab 2 is a demo day, not a first-attempt day

::: {.notes}
~1 min. No lecture content to prepare — this slide exists purely so the deck has a placeholder for this date, matching every other module's session structure. Use actual class time for circulating and debugging, not presenting.
:::

---

## One Last Thing

You have spent a semester learning how to make computation go faster.

But a faster recommendation engine is also a recommendation engine that can serve more users, at lower cost, consuming more energy.

**Speed and scale are not ethically neutral.**

::: {.notes}
~1 min. End the course on this note. EL-05's three prompts are not a postscript — they are the final exam question the course has been building toward.
:::
