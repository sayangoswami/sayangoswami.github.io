---
title: "Module 6: GPU Libraries & Applications"
subtitle: "CuPy, PyTorch, and the Wider Ecosystem — COSC 435"
format: revealjs
---

<!--
INSTRUCTOR NOTES
================
PROMOTED this session (full rewrite). Restructured into three dated sessions
matching schedule.md exactly (Nov 16 CuPy, Nov 18 PyTorch, Nov 23 ecosystem +
capstone kickoff), with explanatory content moved from speaker notes onto
visible slides and time budgets added, following the course-wide promotion
standard.

Nov 18 (PyTorch GPU Basics) is ENTIRELY NEW — previously had zero slide
content anywhere, despite Lab 12 Exercise 5 (PyTorch tensors + forward pass,
added earlier this engagement) and Final Exam A6 both examining it. Nov 23
(GPU ecosystem survey + capstone kickoff) is also ENTIRELY NEW.

The old CuPy-only deck's "Key CuPy Operations" section previously asserted
"your hand-written tiled kernel from Lab 09 is approximately this [cuBLAS],
but slower" as an unverified claim — Lab 12 Exercise 2 was rewritten earlier
this engagement to actually *measure* CuPy (cuBLAS) against the Lab 9 tiled
kernel empirically. That slide now describes the real comparison rather than
asserting an answer. "Lab 12 and the Capstone Bridge" previously listed only
Exercises 1-4, missing Exercise 5 (PyTorch) — corrected, and the Checkpoint
Preview / EL-04 content re-dated and re-placed at the end of the Nov 16
session where it belongs (Quiz 6 opens that session, not this one).

The Nov 23 kickoff deliberately does NOT re-explain team-formation details
(team size, trio variant, grading structure) already covered in
slides/module07-capstone.qmd's own "Team Structure" section — it announces
the kickoff and points forward, matching how other modules avoid duplicating
content that belongs to a later module.
-->

# Session 1 · Nov 16 · CuPy

## Today's Plan

- Quiz 6 first (15 min, Blackboard) — covers all of Module 5
- Then: why you'd reach for a library instead of writing a kernel by hand
- CuPy's pitch — NumPy syntax, GPU execution
- Three CuPy operations you'll use directly in the capstone

::: {.notes}
~1 min. ~35 min of lecture time after the quiz. This session sets up Lab 12 directly — nearly every code example here reappears as a lab exercise two days later.
:::

---

## The Problem with Writing Every Kernel by Hand

- Every kernel this semester (Modules 2-5) has been hand-written: you declared the launch configuration, wrote the indexing, managed shared memory yourself
- That's the right skill to have — but for **standard, well-known operations** (sum, mean, sort, matrix multiply), you'd be re-deriving code that GPU vendors have already spent years hand-tuning
- NVIDIA ships a whole ecosystem of libraries for exactly this: **cuBLAS** (dense linear algebra — matrix multiply, among others), **cuDNN** (deep-learning primitives — convolutions, activations), **cuFFT** (Fourier transforms), **cuRAND** (random number generation), **cuSPARSE** (sparse matrix operations), and more
- **CuPy** is a Python array library that wraps this whole ecosystem behind a NumPy-shaped API — you get the libraries' hand-tuned performance without calling any of them directly

::: {.notes}
~3 min. This directly answers "why does this course teach both Numba and CuPy" — Numba is for when no library does what you need; CuPy is for when one already does, and does it better than you'd write it in a semester.
:::

---

## CuPy: The Pitch

> CuPy is an open-source array library for GPU-accelerated computing with Python. CuPy uses CUDA Toolkit libraries including cuBLAS, cuDNN, cuRAND, cuSolver, cuSPARSE, cuFFT and NCCL to make full use of the GPU architecture.

```python
import numpy as np
import cupy  as cp

# NumPy on CPU
x_cpu = np.array([1.0, 2.0, 3.0], dtype=np.float32)
y_cpu = np.sqrt(x_cpu)

# CuPy on GPU — nearly identical API
x_gpu = cp.array([1.0, 2.0, 3.0], dtype=cp.float32)
y_gpu = cp.sqrt(x_gpu)
```

- Almost all NumPy code ports to CuPy by changing `np.` to `cp.` — the array now lives in GPU memory, but the Python-level API is (almost) unchanged
- Under the hood, `cp.sqrt` dispatches to a real GPU kernel — you're not simulating anything, this genuinely runs on the GPU

::: {.notes}
~2 min. Worth a quick live demo if time allows: take a NumPy one-liner from Module 0, swap the import, and show it just works.
:::

---

## The Three-Step Cycle

```python
import numpy as np
import cupy  as cp

# 1. Move data to GPU
data_cpu = np.random.randn(5000, 200).astype(np.float32)
data_gpu = cp.asarray(data_cpu)

# 2. Compute on GPU — no kernel required
mean_gpu = cp.mean(data_gpu, axis=0)   # column means
std_gpu  = cp.std(data_gpu,  axis=0)
z_gpu    = (data_gpu - mean_gpu) / std_gpu

# 3. Bring the result back
z_cpu = cp.asnumpy(z_gpu)
```

- Same host↔device transfer discipline from Module 2 (`cuda.to_device()`/`copy_to_host()`), just spelled `cp.asarray()`/`cp.asnumpy()`
- Three lines replace what would be an entire shared-memory kernel in Numba — broadcasting, column-wise reduction, and element-wise division are all GPU-accelerated transparently
- This exact pattern is Lab 12 Exercise 1

::: {.notes}
~3 min. Point out explicitly: this is computing a column-wise z-score, the same operation CK-12 checkpoints — worth flagging that Lab 12 and CK-12 are testing precisely this three-line pattern, not something harder.
:::

---

## What Happens Under the Hood

- `cp.asarray()` → the same kind of host→device memory copy as `cuda.to_device()`
- `cp.mean()` → an optimized reduction kernel, dispatched to the underlying CUDA libraries (conceptually similar to the tree reduction you hand-wrote in Module 5, but vendor-tuned)
- `data_gpu - mean_gpu`, `/ std_gpu` → element-wise kernels, generated and launched automatically — no `@cuda.jit`, no launch configuration, no bounds guard written by you
- `cp.asnumpy()` → device→host copy
- CuPy manages device memory allocation, execution streams, and synchronization for you — you only call `cp.cuda.Stream.null.synchronize()` explicitly when you need to *time* something (Module 3's lesson about asynchronous launches still applies underneath)

::: {.notes}
~3 min. Contrast explicitly with Numba: every one of these five steps was something you wrote by hand in Modules 2-5. CuPy's abstraction is higher-level — the cost is less direct control over exactly how each operation is implemented, which is the right trade for "standard" operations and the wrong trade when you need a specific, novel algorithm.
:::

---

## Key Operation: Matrix Multiply

```python
A_gpu = cp.random.randn(1024, 512, dtype=cp.float32)
B_gpu = cp.random.randn(512, 1024, dtype=cp.float32)

# Dispatches to cuBLAS SGEMM — one of the most optimized routines ever written
C_gpu = A_gpu @ B_gpu        # shape (1024, 1024)
```

- `@` on two CuPy arrays calls cuBLAS's SGEMM routine — decades of hand-tuning across every NVIDIA GPU generation, doing tiling, prefetching, and vectorized loads far more aggressively than a semester-long course can teach
- Lab 12 Exercise 2 has you measure this directly against **your own Lab 9 tiled kernel**, not just take "cuBLAS is faster" on faith — you'll get a real number, and explain it in a markdown reflection
- Whichever way that comparison lands, the reasoning matters more than the number: a hand-written kernel like Lab 9's is real, working code — cuBLAS is the same idea, refined far beyond what's practical to hand-tune in one course

::: {.notes}
~3 min. Don't state a winner here — let Lab 12 Exercise 2 be the actual measurement. It's usually close, and being close is itself the interesting result (a correctly-tiled kernel is not that far from a professionally-tuned library, even though the library will typically still win).
:::

---

## Key Operation: Partial Sort with `argpartition`

```python
scores_gpu = cp.random.randn(500, 10_000, dtype=cp.float32)
K = 5

# For each row: find the indices of the K largest values
# Does NOT guarantee order within the K — faster than a full sort
top_k_idx = cp.argpartition(-scores_gpu, K, axis=1)[:, :K]
```

- `cp.argpartition` places the K largest values into the first K positions of each row, without fully sorting them relative to each other — for "give me the top K," you don't need a full sort, and skipping it is faster
- Expected O(N) time per row versus O(N log N) for a full sort — the difference matters when K is much smaller than N, exactly the capstone's situation (K=10, N=100,000)
- This is the capstone's second GPU operation, right after the similarity computation: find the top-K most similar items per query

::: {.notes}
~2 min. This is CK-14 Part B's operation directly. Worth a concrete number: for K=10 out of N=100,000, a full sort does unnecessary extra work ordering 99,990 values nobody asked for.
:::

---

## Key Operation: Random Number Generation

```python
rng = cp.random.default_rng(seed=42)
embeddings = rng.standard_normal((100_000, 64), dtype=cp.float32)

# Normalize to unit vectors
norms = cp.linalg.norm(embeddings, axis=1, keepdims=True)
unit_embeddings = embeddings / norms
```

- CuPy's random number generator runs on the GPU — generating millions of random values is itself a parallel operation, not something worth doing on the CPU first and transferring over
- Normalization via broadcasting is the same syntax you've used in NumPy since Module 0
- This exact pattern generates the capstone's synthetic database and query embeddings — you'll see it directly in CK-13 and CK-14's setup cells

::: {.notes}
~2 min. Worth naming explicitly: this is where the capstone's "100,000 songs, each with 64 numbers" data actually comes from — not a real dataset, a GPU-generated synthetic one, and this is the code that generates it.
:::

---

## CuPy vs. Numba: When to Use Each

| Task | Use |
|---|---|
| Standard operation (sum, mean, sort, matmul) | CuPy |
| Custom operation with no library equivalent | Numba `@cuda.jit` |
| Quick port of existing NumPy code | CuPy |
| Need full control over memory layout and synchronization | Numba |
| Writing a new algorithm (reduction, scan) from scratch | Numba |
| Capstone's similarity + top-K pipeline | Numba for the tiled kernel (CK-13/14), CuPy for `argpartition` |

- This table is Lab 12 Exercise 4's actual question, in table form — there's rarely one universally "correct" tool, only a better fit for a specific situation
- The capstone itself uses **both**: a hand-written Numba kernel for the similarity computation (where the tiling optimization is the point of the exercise) and CuPy for top-K selection (a standard operation with no reason to reinvent it)

::: {.notes}
~3 min. Emphasize there's no single right answer for most rows — this table is a starting heuristic, and Lab 12 Exercise 4 asks students to reason through a specific scenario (a 10M×128 real-time z-score pipeline) rather than just recite the table.
:::

---

## Interoperability: CuPy and Numba Together

```python
import numba.cuda as cuda

x_cp = cp.zeros(1000, dtype=cp.float32)

# CuPy arrays live in ordinary CUDA device memory — Numba can use them directly
my_numba_kernel[blocks, threads](x_cp)   # works, no conversion needed

# After Numba modifies it in place, CuPy sees the updated result immediately
result = cp.sum(x_cp)
```

- CuPy and Numba both allocate from the same GPU memory — a CuPy array can be passed straight into a `@cuda.jit` kernel, and a Numba-allocated device array can be wrapped by CuPy, with **no copy** in either direction
- This means the CuPy-vs-Numba choice (previous slide) isn't all-or-nothing per project — you can mix both within a single pipeline, exactly as the capstone does
- Practical implication: you never need to round-trip through host memory just to switch which tool operates on your data next

::: {.notes}
~2 min. This is the practical payoff of the previous slide's table — students don't have to pick one tool and commit to it for the whole capstone. Reinforce: zero-copy handoff between the two, not "convert and copy."
:::

---

## This Week

- **Lab 12 (Fri, Nov 20):** All five exercises — CuPy z-score, CuPy-vs-NumPy-vs-Numba matmul timing, CuPy `argpartition` top-K, a CuPy-vs-Numba reflection, and PyTorch tensors/forward pass — **CK-12** same day
- **Nov 18 (next lecture):** PyTorch — a different GPU library, built for a different purpose (neural networks) than CuPy's general-purpose array operations

::: {.notes}
~1 min. Quick pointer slide.
:::

---

## Checkpoint Preview

Checkpoint CK-12 will ask you to:

- Transfer a NumPy matrix to the GPU as a CuPy array
- Compute a column-wise z-score entirely on the GPU using CuPy broadcasting
- Retrieve the result back to a NumPy array on the CPU
- (Ungraded reflection: when would you choose CuPy over a custom Numba kernel?)

::: {.notes}
~1 min. 20-minute timed, paired with Lab 12 (Nov 20). CuPy-only by instructor decision — PyTorch is practiced in this lab (Exercise 5) but not independently checkpointed, since a 20-minute window doesn't comfortably fit two separate library skills (see checkpoints/ck12-cupy-lab.md's instructor notes).
:::

---

## EL-04

- **Due Sun, Nov 1** (before this session — already assigned): read Spotify's "From Idea to Execution" on embeddings, watch Vox's "How Netflix Recommends Movies," and reflect on the embedding connection to the capstone, the scale challenge, and the ethics of recommendation optimization
- Directly motivates the capstone's structure: it's a recommendation system, built the same way real ones are, just at a smaller scale

::: {.notes}
~1 min. This is a pointer/reminder slide, not a new assignment — EL-04's due date (Nov 1) falls well before this Nov 16 lecture. Worth a brief show-of-hands on who's completed it, since it directly primes the capstone framing used throughout this module.
:::

---

# Session 2 · Nov 18 · PyTorch GPU Basics

## Today's Plan

- Where PyTorch fits: a library built for neural networks, not general array math
- Tensors — PyTorch's array type, and moving them to the GPU
- Autograd — what it is conceptually, and why we won't be using it this session
- A complete forward pass through a small network, on the GPU

::: {.notes}
~1 min. Full 50-min session, no quiz. This session's code is deliberately lighter than Nov 16's — PyTorch's role in this course is intentionally narrow (inference only), per syllabus.md's own scope note that C6.2 is "one lab, reinforced at Capstone," not a deep dive.
:::

---

## Where PyTorch Fits

- CuPy (Nov 16) is a **general-purpose** GPU array library — anything you'd do with NumPy, CuPy can likely do on the GPU
- **PyTorch** is a GPU library built specifically for **machine learning**: defining neural networks, running them, and (when needed) training them
- The GPU-to-ML connection is direct: modern AI models (the kind behind image generators, recommendation systems, chatbots) are built and run almost entirely using libraries like PyTorch, on GPUs — this is a large part of *why* GPU programming is such an in-demand skill today (Module 1's opening pitch, made concrete)
- This course teaches PyTorch at a deliberately narrow slice: moving data to the GPU and running a pretrained model's forward pass — not training a model from scratch, which is its own, much longer topic

::: {.notes}
~2 min. Set expectations explicitly: this is one lecture and one lab exercise, not a machine-learning course. The goal is recognizing the GPU-programming ideas (device transfer, GPU execution) inside a tool students will likely encounter again, not becoming a PyTorch expert.
:::

---

## Tensors: PyTorch's Array Type

```python
import torch

# A tensor looks and acts like a NumPy array
x_cpu = torch.randn(32, 128)          # 32 samples, 128 features each
print(x_cpu.shape, x_cpu.device)      # torch.Size([32, 128]) cpu

# Move it to the GPU — PyTorch's version of cuda.to_device() / cp.asarray()
x_gpu = x_cpu.to('cuda')
print(x_gpu.device)                   # cuda:0
```

- A PyTorch **tensor** is PyTorch's array type — conceptually the same idea as a NumPy array or a CuPy array, just PyTorch's own implementation, built with neural-network workloads in mind
- `.to('cuda')` is PyTorch's device-transfer call — the same underlying idea as `cuda.to_device()` (Module 2) and `cp.asarray()` (Nov 16), spelled differently because it's a different library
- `.cpu()` is the reverse — PyTorch's equivalent of `.copy_to_host()` / `cp.asnumpy()`. Three libraries, three spellings, the same host↔device transfer concept every time

::: {.notes}
~3 min. Explicitly name the pattern-matching across libraries — students should recognize "oh, this is the same host/device transfer idea from Module 2 and Nov 16, just PyTorch's syntax for it" rather than treating this as unrelated new material.
:::

---

## Autograd: What It Is (Conceptually)

- PyTorch's other major feature, beyond tensors, is **autograd** — automatic differentiation
- When training a neural network, you need the *gradient* (how much the output would change if you nudged each internal number slightly) to know which direction to adjust the model's parameters to make it more accurate
- Autograd tracks every operation performed on a tensor (as long as that tensor has `requires_grad=True`), building a record of the computation as it happens — then `.backward()` walks that record backward, computing every needed gradient automatically via the chain rule
- This is genuinely one of PyTorch's most important features for **training** a model — but this course does not train models, only runs already-trained ones (next slide explains why that matters)

::: {.notes}
~3 min. This is deliberately conceptual, not hands-on — students won't write autograd code in this course. The goal is recognizing the term and roughly what problem it solves, since "autograd" is schedule.md's own committed sub-topic and a term students will encounter constantly if they go further with PyTorch.
:::

---

## Inference: Running a Model Without Training It

```python
with torch.no_grad():           # disable autograd — we're not training
    logits_gpu = model(x_gpu)   # the forward pass
```

- **Inference** means running a model to get its output, without updating the model itself — exactly what happens when a trained image classifier labels a new photo, or a trained recommendation model scores a new user
- `torch.no_grad()` explicitly turns autograd **off** for the code inside it — since inference never needs a gradient, tracking the computation graph would only cost extra memory and time for no benefit
- This is the single most important line for this course's PyTorch use: it's the difference between "just get the answer" (inference, what we do) and "also prepare to compute gradients" (training, what we don't do)

::: {.notes}
~2 min. Tie directly back to the previous slide: autograd is a real, powerful feature — and the correct move whenever you're *not* training is to explicitly disable it. This is the practical payoff of understanding autograd conceptually, even without using it.
:::

---

## A Complete Forward Pass

```python
import torch
import torch.nn as nn

torch.manual_seed(12)

# A tiny 2-layer network — imagine this is a pretrained classifier
model = nn.Sequential(
    nn.Linear(128, 64),
    nn.ReLU(),
    nn.Linear(64, 10),
).to('cuda')                      # move the model's parameters to the GPU too

x_cpu = torch.randn(32, 128)      # 32 samples, 128 features each

x_gpu = x_cpu.to('cuda')          # move the input to the GPU
with torch.no_grad():
    logits_gpu = model(x_gpu)     # forward pass — runs entirely on the GPU

logits_cpu = logits_gpu.cpu()     # bring the result back
print(logits_cpu.shape)           # torch.Size([32, 10])
```

- `nn.Sequential` chains layers together — here, a linear layer (128 inputs → 64 outputs), a ReLU activation, and a second linear layer (64 → 10 outputs, e.g. 10 class scores)
- **Both** the model's parameters (`.to('cuda')` on the model) and the input data (`.to('cuda')` on `x_cpu`) must be on the same device before `model(x_gpu)` runs — a common early error is forgetting one of the two and getting a device-mismatch error
- This is Lab 12 Exercise 5's exact pattern, with the three `# YOUR CODE HERE` lines being the `.to('cuda')` call, the forward pass, and the `.cpu()` call

::: {.notes}
~4 min. Walk through the shape transformation explicitly: 32×128 in, through a 128→64 layer, through ReLU (same shape, just zeroes out negatives), through a 64→10 layer, out as 32×10. This is the exact code Lab 12 Exercise 5 has students complete.
:::

---

## This Week

- **Lab 12 (Fri, Nov 20):** Exercise 5 — complete the forward-pass pattern above; the lab's other four exercises are CuPy (previewed Nov 16)
- **Nov 23 (next lecture):** A survey of the wider GPU ecosystem beyond CUDA, plus the capstone kickoff

::: {.notes}
~1 min. Quick pointer slide. No separate checkpoint preview here — PyTorch isn't independently checkpointed this semester (see Nov 16's Checkpoint Preview note).
:::

---

# Session 3 · Nov 23 · The GPU Ecosystem & Capstone Kickoff

## Today's Plan

- Beyond CUDA: what other GPU programming ecosystems exist, and why they exist
- Where the field is heading, and what that means for your career
- Capstone kickoff: the project you've been building toward all semester officially starts today

::: {.notes}
~1 min. This is the last Module 6 session before Fall Break (Nov 25/27, no class) — Module 7's capstone-focused lectures don't resume until Nov 30. Keep the ecosystem survey brisk; leave real time for the kickoff logistics, since team formation needs to happen before the break.
:::

---

## CUDA Isn't the Only GPU Language

- Everything this semester has used **CUDA**, NVIDIA's GPU programming platform — for good reason: it's the most mature, most widely used, and has the deepest tooling and library ecosystem (Nov 16's cuBLAS/cuDNN/etc.)
- But CUDA only runs on NVIDIA hardware. Several other approaches exist for programming GPUs from other vendors, or across vendors:
  - **HIP** (AMD): a CUDA-like C++ API designed so that CUDA code can be automatically translated (or written portably) to also run on AMD GPUs
  - **OpenACC**: a directive-based approach — you annotate ordinary C/C++/Fortran loops with compiler pragmas (comments the compiler interprets), and the compiler generates the parallel GPU code for you, rather than you writing kernels by hand
  - **WebGPU**: a modern, cross-vendor API for running GPU compute (and graphics) directly inside a web browser — works on NVIDIA, AMD, Intel, and mobile GPUs alike, all from JavaScript
  - **Apple Metal**: Apple's own GPU programming API, used across macOS and iOS devices

::: {.notes}
~4 min. Frame this as "the landscape," not "you need to learn these too" — a brief tour is the point, not depth. If time allows, ask if anyone has encountered any of these already (WebGPU shows up in browser-based ML demos; Metal is relevant to anyone doing iOS/Mac development).
:::

---

## Why So Many Approaches?

- **Vendor lock-in** is the core tension: CUDA's maturity comes from being NVIDIA-exclusive, which is exactly why AMD, cross-platform, and browser-based alternatives exist — each trades some of CUDA's tooling depth for running on hardware CUDA can't reach
- **Abstraction level** varies deliberately: OpenACC's directive-based approach trades some performance control (you're not hand-tuning launch configuration the way this course has all semester) for a much lower barrier to parallelizing existing code
- **Where code runs** matters increasingly: WebGPU exists because browser-based and edge-device GPU compute is a growing category CUDA (a desktop/server/Linux-first platform) doesn't reach well
- None of these replace what you've learned this semester — the *concepts* (thread parallelism, memory hierarchy, minimizing host↔device transfer, avoiding divergence) transfer directly to every one of them, even though the exact syntax differs

::: {.notes}
~2 min. This is the reassuring payoff of the survey: students haven't learned "a CUDA course," they've learned GPU programming concepts that CUDA happens to be an excellent vehicle for teaching. That transfers.
:::

---

## Where the Field Is Heading, and Career Context

- GPU programming skill has moved from a niche graphics/HPC specialty to one of the most broadly in-demand skills in computing — spanning ML/AI infrastructure, scientific computing, finance, genomics, robotics, and real-time graphics
- The concepts from this course (occupancy, coalescing, shared-memory tiling, reduction/scan patterns, library vs. custom-kernel judgment) are the same concepts a working GPU engineer reasons about daily, regardless of which specific language or vendor's hardware they're targeting
- The library-vs-custom-kernel judgment call from Nov 16 (C6.3) is itself a career-relevant skill: most GPU-programming jobs involve far more "which existing tool solves this" reasoning than "write a kernel from scratch" — knowing when *not* to hand-roll something is as valuable as knowing how

::: {.notes}
~2 min. Good moment to connect back to Module 1's opening pitch ("GPU programming is one of the most in-demand skills in computing") — this slide is that promise, revisited with a full semester of concrete skill behind it instead of just a claim.
:::

---

## The Capstone Starts Today

- Recall the Day-1 teaser (Module 0/1): a music recommendation system — 100,000 songs, each a 64-number "fingerprint," matched against user preferences via a dot product, at scale
- That is the capstone. Every module this semester has been building toward implementing it: kernels (Module 2), memory optimization (Modules 3-4), parallel patterns (Module 5), and the libraries (this module) all combine into one working GPU pipeline
- Two milestones ahead: **CK-13** (due Tue, Dec 1 — problem scoping, baseline, working GPU connection) and **CK-14** (due finals week — full implementation, profiling, and optimization). Dec 4 and Dec 11 are in-class work/demo days connected to each milestone, not the deadlines themselves
- Full details — team structure, grading philosophy, the optimization challenge, academic integrity expectations — are covered in Module 7's own lecture, starting Nov 30

::: {.notes}
~2 min. Deliberately brief — Module 7 owns the full depth of this material (team structure, grading, Part F's optimization challenge). Today's job is the announcement and the logistics needed *before* the break, not a repeat of that content.
:::

---

## Team Formation — Do This Before the Break

- Teams of 2 (one trio permitted if enrollment is odd) — declare your team by the end of this week, before Fall Break
- Use the time over break (Nov 25-27, no class) to at least discuss problem framing with your teammate — Module 7's CK-13 milestone (due Dec 1) comes up fast once class resumes Nov 30, only a few days later
- If you don't yet have a teammate, say so now — instructor will help match remaining students before the break, not after

::: {.notes}
~2 min. This is the one piece of today's content that's genuinely time-sensitive — leave a few minutes at the end of class specifically for students to find partners in the room, rather than leaving it to chance over a message board after class ends.
:::

---

## Looking Ahead

- **Nov 25 & 27:** Fall Break — no class
- **Nov 30 (Module 7 resumes):** GPU-accelerated image processing walkthrough, then ML inference on GPU (Dec 2) — full capstone depth begins here
- **Dec 1:** CK-13 due (Milestone 1) — just before Dec 4's in-class Capstone Lab 1 work session

::: {.notes}
~1 min. Closing pointer slide.
:::
